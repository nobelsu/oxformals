/**
 * Feed constants shared by the Convex backend and the client. Kept
 * dependency-free (no `@/` alias imports) so the Convex bundler can pull it in.
 */
export const MAX_FEED_COMMENT_LENGTH = 500;
